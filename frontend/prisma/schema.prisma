// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

// Looking for ways to speed up your queries, or scale easily with your serverless or edge functions?
// Try Prisma Accelerate: https://pris.ly/cli/accelerate-init

generator client {
  provider   = "prisma-client-js"
  engineType = "library"
}


datasource db {
  provider = "postgresql"
}

model User {
  id              String   @id @default(uuid())
  email           String   @unique
  name            String?
  password        String
  avatarUrl       String?
  isAdmin         Boolean  @default(false)
  ratings         Rating[]
  recommendations Recommendation[]
  createdAt       DateTime @default(now())
  updatedAt       DateTime @updatedAt
}

model Genre {
  id      String   @id @default(uuid())
  name    String   @unique
  titles  Title[]  @relation("TitleGenres")
}

model Cast {
  id      String   @id @default(uuid())
  name    String
  titles  Title[]  @relation("TitleCast")
}

model Title {
  id              String   @id @default(uuid())
  type            String   // "movie", "show", "anime", "cartoon"
  title           String
  description     String?
  year            Int?
  posterUrl       String?
  ratings         Rating[]
  recommendations Recommendation[]
  createdAt       DateTime @default(now())
  updatedAt       DateTime @updatedAt

  genres      Genre[] @relation("TitleGenres")
  cast        Cast[]  @relation("TitleCast")
  tmdbRating  Float?
  imdbRating  Float?
  keywords    String? // comma-separated or JSON

  @@index([type])
}

model Rating {
  id        String   @id @default(uuid())
  user      User     @relation(fields: [userId], references: [id], onDelete: Cascade)
  userId    String
  title     Title    @relation(fields: [titleId], references: [id], onDelete: Cascade)
  titleId   String
  score     Int
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@unique([userId, titleId])
}

model Recommendation {
  id        String   @id @default(uuid())
  user      User     @relation(fields: [userId], references: [id], onDelete: Cascade)
  userId    String
  title     Title    @relation(fields: [titleId], references: [id], onDelete: Cascade)
  titleId   String
  reason    String?
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}
